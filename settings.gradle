// 1. Tells Gradle where to find Gradle PLUGINS (like Shadow)
pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://jitpack.io' }
        mavenCentral()
    }
}

rootProject.name = 'JTemplate'